import SwiftUI

extension Color {
    /// Couleur de marque utilisée pour les titres de section et badges d'en-tête
    /// dans toutes les fenêtres de l'app (ex: `AddAppSheet`, `AboutView`).
    static let switchboardAccent = Color(red: 0x04 / 255, green: 0x09 / 255, blue: 0x43 / 255)
}
